version: '3.8'

services:
  beszel:
    image: henrygd/beszel:latest
    container_name: beszel
    restart: unless-stopped
    ports:
      - "4000:3000"  # Web界面端口
    volumes:
      - ./beszel_data:/app/data
    environment:
      - BESZEL_ADMIN_USER=admin
      - BESZEL_ADMIN_PASSWORD=admin